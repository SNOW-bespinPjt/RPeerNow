<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.peernow360.mappers.ISprintMapper">

    <insert id="createSprintInfo"
            parameterType="com.example.peernow360.dto.SprintDto">

        INSERT INTO
            SPRINT(project_no,
                   title,
                   detail,
                   start_date,
                   end_date,
                   reg_date,
                   mod_date)
        VALUES
            (#{project_no},
             #{title},
             #{detail},
             #{start_date},
             #{end_date},
             NOW(),
             NOW())

    </insert>
    
    <select id="sprintDetailInfoByNo" parameterType="Integer" resultType="com.example.peernow360.dto.SprintDto">

        SELECT
            *
        FROM
            SPRINT
        WHERE
            project_no=#{project_no}

    </select>

    <update id="updateSprintInfo" parameterType="com.example.peernow360.dto.SprintDto">

        UPDATE
            SPRINT
        SET
            title = #{title},
            detail = #{detail},
            start_date = #{start_date},
            end_date = #{end_date},
            mod_date = NOW()
        WHERE
            no = #{no}

    </update>

    <delete id="removeSprintInfo" parameterType="Integer">

        DELETE FROM
                   SPRINT
        WHERE
            no = #{no}

    </delete>


    <insert id="insertUserMember"
            parameterType="com.example.peernow360.dto.UserMemberDto">

        INSERT INTO USER(id,
                         pw,
                         name,
                         mail,
                         phone,
                         team,
                         image,
                         grade,
                         reg_date,
                         mod_date)
        VALUES (#{id},
                #{pw},
                #{name},
                #{mail},
                #{phone},
                #{team},
                #{image},
                #{grade},
                NOW(), NOW())

    </insert>

    <update id="updateUserPW"
            parameterType="com.example.peernow360.dto.UserMemberDto">

        UPDATE
            USER
        SET
            pw = #{pw}
        WHERE
            id = #{id}

    </update>

    <insert id="insertRefreshToken" parameterType="map">

        INSERT INTO
            TOKENS(user_id, refreshToken)
        VALUES
            (#{user_id}, #{refreshToken})

    </insert>

    <select id="findByToken" parameterType="String" resultType="String">

        SELECT
            refreshToken
        FROM
            TOKENS
        WHERE
            user_id = #{user_id}

    </select>

    <update id="modifyRefreshToken" parameterType="map">

        UPDATE
            TOKENS
        SET
            refreshToken = #{refreshToken}
        WHERE
            user_id = #{user_id}

    </update>

    <select id="compareRefreshToken" parameterType="String" resultType="boolean">

        SELECT
            count(*)
        FROM
            TOKENS
        WHERE
            refreshToken = #{refreshToken}

    </select>

    <insert id="insertBlacklist" parameterType="String">

        INSERT INTO
            BLACKLIST_TOKENS(user_id, blacklist)
        SELECT
            t.user_id, t.refreshToken
        FROM
            tokens t
        WHERE
            refreshToken = #{refreshToken}

    </insert>

    <delete id="removeRefreshToken" parameterType="String">

        DELETE FROM
                   TOKENS
        WHERE refreshToken = #{refreshToken}

    </delete>

    <delete id="removeAccountInfo" parameterType="String">

        DELETE FROM
                USER
        WHERE
            id = #{id}

    </delete>

    <update id="modifyAccountInfo" parameterType="com.example.peernow360.dto.UserMemberDto">

        UPDATE
            USER
        SET
            name = #{name} ,
            mail = #{mail} ,
            phone = #{phone} ,
            team = #{team} ,
            image = #{image},
            mod_date = NOW()

        WHERE
            id = #{id}

    </update>

    <select id="selectBlackListToken" parameterType="String" resultType="boolean">

        SELECT
            count(*)
        FROM
            BLACKLIST_TOKENS
        WHERE
            blacklist = #{refreshToken}

    </select>


</mapper>